
name : push1

on : push

jobs:
  success :
    runs-on : ubuntu-latest
    steps :
      # - uses: Wandalen/wretry.action@v3
      # - name: pre
      #   uses: dmvict/wretry.action/pre@alt_conditions
      #   with:
      #     action: dmvict/test.action@docker_conditions
      #     branch: js_action
      #     with: |
      #       string: -al
      #       bool-like: /
      #     attempt_limit: 4
      #     attempt_delay: 3000
      - name: main
        id: mmmm
        uses: Wandalen/wretry.action/main@master
        with:
          action: dmvict/test.action@alt
          # with: |
          #   string: -al
          #   bool-like: /
          attempt_limit: 4
          attempt_delay: 3000
      - name: command
        run: echo -en "${{ fromJSON( steps.mmmm.outputs.outputs ) }}"
      - name: command2
        run: echo -en "${{ fromJSON( steps.mmmm.outputs.outputs ).string }}"
      # - run: echo -n "${{ fromJSON( steps.main.outputs ).string }}"
