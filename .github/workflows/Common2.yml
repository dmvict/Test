
name : command

on: push

jobs :

  build:
    uses : dmvict/Test/.github/workflows/Common.yml@master
    with :
      matrix : '{ "os" : [ "ubuntu-latest", "windows-latest", "macos-latest" ], "version" : [ "14.x" ] }'
