
name : push1

on : push

jobs:
  success :
    runs-on : ubuntu-latest
    steps :
      - uses: 'dmvict/wretry.action@exp'
        with:
          action: ./github/actions
          attempt_limit: 4
          attempt_delay: 1000
