
name : push

on : [ push ]

jobs :

  fast :
    runs-on : ubuntu-latest
    steps :
      # - run : echo ${{ secrets }}
      # - run : echo -e "${{ toJson( secrets ) }}" > file.json
      - uses: actions/upload-artifact@v3
        with:
          name: save
          path: |
            /home/runner/work/_temp
      - run: tree /home/runner
