
name : push1

on : push

jobs:
  success :
    runs-on : ubuntu-latest
    steps :
      - uses: dmvict/wretry.action@alt
        with:
          action: dmvict/test.action@alt
          with: |
            singleline: |
              some

              foo

                bar
            multiline: |
              some

              foo

                bar
            true_v: true
          attempt_limit: 3
          attempt_delay: 2000
